FROM python:3.14-slim-bookworm

RUN apt-get update \
 && apt-get install -y --no-install-recommends curl ca-certificates \
 && rm -rf /var/lib/apt/lists/*
WORKDIR /opt/abg
COPY probe.py /opt/abg/probe.py
ENV ABG_PROVIDER=curl
ENTRYPOINT ["python3","/opt/abg/probe.py"]
