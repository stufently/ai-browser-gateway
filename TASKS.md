# Рабочий журнал `ai-browser-gateway`

Единственный источник правды по статусу работ. Изменения кода/поведения — в
`CHANGELOG.md`, сюда не дублируются. Записи старше ~30–60 дней уезжают в
`docs/worklog-archive/YYYY-MM.md`.

## За владельцем (я сам сделать не могу)

- [x] **Поддомен под свой CF-стенд — выкачен (14.09.2026).** Токен CF не нужен:
  октodns-репо `~/gitlab/9qw/octodns` держит `CF_API_TOKEN` в CI, apply идёт сам
  на push в `main`. Добавил `abg-cf.example.net` A `192.0.2.10` (stand-host), `proxied:
  true` — `dump/example.net.yaml`, коммит `1d92667`. Цель `own-cf-stand` включена в
  `targets.toml` (`valid=false` до настройки). ОСТАЁТСЯ (executor): сервис на
  stand-host:443 под этот хост + WAF-правило Managed Challenge и Turnstile на тестовых
  ключах — тогда стенд даёт воспроизводимость, которой у чужих целей нет.
- [x] **Cloudflare Browser Run — снят (14.09.2026).** Владелец: «кажется тебе это
  не надо». Провайдер CF Browser Run из меню и из замера фазы 1 исключается,
  строка «не измерено» больше не висит.
- [x] **Второй egress для фазы 1 — решено (14.09.2026).** GoldProxy снят с
  повестки владельцем («больше нет и не будет»). Штатный второй egress — пул
  прокси `ms1-15.example.net` (инфраструктура владельца, кредов ждать не надо). Это
  закрывает вопрос «отличить „инструмент не справился“ от „адрес не пустили“»:
  хост сидит на хостинговом ASN без репутации (`AS206996 ZAP-Hosting`), теперь
  есть с чем сравнить. Остаток — НА НАШЕЙ стороне: завести пул в прокси-слой
  bench (M5 уже умеет прокси) и прогнать сравнительный замер тем же образом и
  отпечатком с обоих адресов.
- [x] **Живой рилс для `login-instagram` — выдан (14.09.2026).** Владелец дал
  `https://www.instagram.com/reels/DdJ-MY6OITt/`. Прописан в `targets.toml`,
  `valid=true`. Отдельно: проверить инструменты именно на этом рилсе (доберётся
  ли до контента за стеной логина) — задача для executor.
- [ ] **Какие ещё боевые цели из рабочих проектов добавить.** Сейчас взяты
  `bizprofile.net` (снят с мониторинга check-sites из-за CF, возврат намечен на
  12.09.2026), `instagram.com` (три задачи бэклога упираются в стену логина) и
  магазины hqd как контроль «чистый nginx».

## Готовность к использованию — поручение 14.09.2026

- [x] **Разведка перед M9.** Исходная база `0f800ed20162394079484b64df483114ad598e75`.
  Задание владельца прочитано целиком:
  `/home/user/gitlab/9qw/tg-claude-userbot/tmp/abg-coord-task-20260914.md`.
  Задание Grok сохранено в `docs/specs/m9-recon.md` (копия запуска:
  `/home/user/.cache/abg-coord-20260914/recon.md`);
  отдельный клон: `/home/user/exec-clones/abg-ready-recon-20260914`,
  push в origin отключён. Первый запуск отклонён журналом: оба слота `gk`
  заняты (`t-5885192dfe77-a01`, `t-f4f31014a661-a01`). После освобождения
  слота разведка выполнена Grok на `4af46c5`, чистый клон. Результат:
  `/home/user/.cache/abg-coord-20260914/recon-result.md`, SHA256
  `bcddcd468c76d3bc792f9deab9a1accb7575052daf3e59203e77feaded988b21`.
  Панель `gk-abg-ready-recon-20260914` закрыта, подготовка принята
  (`t-08a931001841-a01`); продуктовая веха этим не принимается.
  Остатки окон 5h/weekly Grok и Spark неизвестны, quota error не получен.
- [ ] **M9 — реальный транспорт страницы.** Спека `docs/specs/m9-real-fetcher.md`.
  Монетка `auto --select-only` один раз выбрала Grok; запись
  `/home/user/.cache/abg-coord-20260914/m9/selection.json`. Реализация ещё
  не запущена. Клон `/home/user/exec-clones/abg-m9-real-fetcher-20260914`,
  BASE_SHA `4af46c52a4376d903f0fc49f41d211f94149a6c8`.
  Независимый пробник готовит Spark: `cx-abg-m9-probes-20260914`, клон
  `/home/user/exec-clones/abg-m9-probes-20260914`; задание и сторож в
  `/home/user/.cache/abg-coord-20260914/m9/` (`probe-task.md`, `probe-watch.log`).
  Первый Spark завершился преждевременно с черновиком `0c16649` без
  reference/evasive и отчёта. Координатор прочитал пробник и вернул одним
  списком ошибки контракта и недостающие доказательства; продолжение тем же
  Spark по `probe-continue.md` остановилось по квоте (rc=1).
  Последние байты Spark сохранены координатором без правок в локальный коммит
  `d0b1a92`, ветка `m9-probes`; оба коммита НЕ приняты и не опубликованы.
  Есть также `m9/probe-spark-quota-checkpoint.patch`. Рабочее дерево клона чистое.
  В клоне реализации обнаружилась отдельно скопированная непринятая версия
  пробника (SHA256 `139ae328e43d959f1add216d1051bfc84ea132b7b315347b410ecfaa607cd169`);
  она убрана с пути исполнения и сохранена в cache как
  `m9/unaccepted-probe-found-in-implementation-clone.py`. Не использовать её.
  Пробник ещё не принят и не заморожен: в частности, проверка env всё ещё
  требует конкретную форму dict вместо значения ABG_PROXY; правильный
  эталон и обходные варианты не подтверждены, `probe-result.md` отсутствует.
  Последняя фактическая квота: **Spark usage limit**, ошибка предлагает
  **20.09.2026 15:59** (в самой ошибке часовой пояс не указан); числовые остатки
  5h/weekly отдельно неизвестны. Лог `m9/spark-quota.log`. Grok quota error
  не выдавал, его остатки неизвестны. Обычный Codex вместо Spark не запускался.
  Попытка `t-1407775d2ba4-a01` помечена blocked; наша Spark-панель и оба
  сторожа завершены. Фоновых запусков/автоповторов этой задачи нет.
  Следующий шаг: пакет `probe-result.md`, проверка baseline/reference/evasive,
  заморозка SHA пробника в спеке, повторный preflight и запуск именно `gk`
  (не новая монетка). У M9 шесть AC; продуктовая лестница без sentinel вынесена
  в следующую веху, затем API, сервис/CLI и egress/live.

### Handoff: ожидание независимого проверяющего

До reset Spark реализацию M9 не запускать: без валидированного независимого
пробника её предполёт не закончен. Передача пробника будущему автору Grok
нарушила бы независимость. Нового разрешённого шага реализации сейчас нет;
проект **не готов к использованию**, ядро продукта остаётся M7/M8.

После reset одна обычная попытка тем же Spark, повторную quota error не
циклить. Подготовлен точный файл продолжения:
`/home/user/.cache/abg-coord-20260914/m9/probe-resume-after-reset.md`.
Команда:
`bash /home/user/gitlab/9qw/tg-claude-userbot/scripts/launch_executor.sh spark /home/user/exec-clones/abg-m9-probes-20260914 --task-file /home/user/.cache/abg-coord-20260914/m9/probe-resume-after-reset.md`.
После старта заново подключить `watch_executor_panes.sh` с выводом в файл.
После готового пробника проверить изменения/эталоны, доставить только принятый
файл в клон M9, заморозить его hash. Перед реализацией сверить BASE_SHA спеки
с фактическим HEAD клона (документация main продвинулась); при обновлении клона
обновить все ссылки на базу до запуска, не менять её во время исполнения.
Запуск реализации — выбранный `gk`, не новая монетка. Затем обязательные два
ревью, независимые мутации Spark, полный пакет и одна приёмка координатора.
- [ ] **M9 и далее (границы уточняются разведкой):** настоящий fetcher поверх
  раннера и измеренная лестница со Scrapling; HTTP API с токеном, форматом
  text/html/markdown, бюджетом и ограничением браузеров; сервис на stand-host и CLI
  с режимами cf-fetch; прокси `ms1-15.example.net` и сравнительный замер; сквозной
  прогон развёрнутого сервиса по всем `valid=true` целям и README.
  Спека следующей вехи пишется после приёмки предыдущей. Каждая веха проходит
  независимую приёмку и мутации, затем commit/push. Потребителей cf-fetch
  автоматически не переключать. DNS, внешнюю публикацию и изменения чужих
  сервисов оставить за владельцем. CF-стенд и дополнительные инструменты —
  после готового пути fetcher/API/service/CLI.

## Остальной бэклог

- [ ] **Инструменты из рилса `DdJ-MY6OITt` — что ещё взять в проект.** Рилс
  (`zhilnikov_it`, 11.09.2026) целиком про Scrapling: «выглядит как настоящий
  браузер», «чинит себя сам при смене вёрстки», «подключается к Claude Code и
  Codex». Первое уже замерено вехой M8 (incremental 1 на `cf-bizprofile`), в
  ядро не встроено. Разобрать остальное по `scrapling==0.4.15`:
  1. **Встроить `scrapling` в меню провайдеров `gateway/`** ступенью после
     `patchright` — единственное, что M8 доказал числом. Решение — после
     сравнительного замера со второго egress (`ms1-15`), иначе «пробил CF» не
     отличить от «адрес пустили».
  2. **MCP-сервер `scrapling[ai]`** (`mcp>=2.0.0`) — сравнить с нашим
     `cf-fetch` для агентов; вероятнее, что шлюзу нужен свой MCP поверх
     лестницы, а не чужой поверх одного инструмента.
  3. **Адаптивные селекторы (`auto_save` / `adaptive=True`)** — слой извлечения
     данных, шлюз отдаёт страницу, а не поля. Для шлюза вне скоупа; оценить для
     проектов-потребителей (скраперы магазинов, мониторинги).
  4. Spiders / AutoThrottle / ротация прокси — краулинг-фреймворк, нам не нужен,
     записано, чтобы не разбирать повторно.

## DONE

- [x] **Веха M8 — провайдер `scrapling`, живая цель `login-instagram`**
  (14.09.2026, слита `a57be16`). Перекрёстные мутации — 12 выживших закрыты
  тестами. Числа — `docs/research/05-scrapling.md`.

- [x] **Веха M7 — ядро шлюза: лестница как код** (14.09.2026).
  Добавлены `gateway/`, меню провайдеров, исполнение существующего правила
  эскалации, квалификация свежести входов, след попыток и общий бюджет времени;
  поведение проверяется поддельным fetcher'ом, тестами и мутационным гейтом.
  Следующей вехе остаются настоящий fetcher поверх `bench.runner.execute`
  и HTTP-API; провайдеры и измеренное правило эскалации сохранены.

- [x] **Веха M6 — правило отбора по нескольким измерениям** (07.09.2026, слита
  `6f1c53d`). Исполнитель Grok, ревью Codex и Antigravity, зонд постановщика.
  Отчёт на умолчаниях противоречил сам себе: «wayback 220,61 ч», «rss 0,02 ч» и
  тут же «rss — исключить». Порядок внутри тира был порядком строк в реестре;
  на шести перестановках вердикт для `rss` менялся три к трём.
  **Первый прогон свойства не дал**, хотя машинная приёмка была 12/12: критерии
  звали фикстуру исполнителя, а в ней ни у одного провайдера не было двух
  полезных клеток — тест на 50 перестановок зеленел вхолостую. Отсюда правило,
  которое теперь применяю всегда: **критерий зовёт МОЙ зонд с вшитым sha256**,
  а не тесты проверяемого. Зонд нашёл четыре дефекта за один прогон.
  На приёмке захода нашёл ещё три сам и правил их сам:
  один протухший замер провайдера поглощал его же свежий (клетка считалась
  покрытой); класс `Decision` с `len()==3` и итерацией на два поля (`list()`
  молча терял измерение); тест на разрыв ничьей именем ловил пропажу разрыва
  лишь в 7 прогонах из 12 — зависимость от `PYTHONHASHSEED` через порядок
  множества, переписан на восемь имён.
  **Две ошибки были мои:** AC-627 запрещал слова в ЛЮБОМ изменённом файле,
  включая саму спеку, которая обязана их называть, — исполнитель обошёл
  невыполнимый критерий, добавив спеку в `.gitignore` (и доложил). Спека
  возвращена в git, `docs/specs/` исключён. И мой зонд читал решение
  распаковкой в два поля, то есть проходил ровно благодаря классу `Decision`;
  теперь читает индексом.

- [x] **Веха M5 — прокси и `environment_error`** (07.09.2026, слита `5e3bbc6`).
  Исполнитель Grok, два независимых прохода Codex (ревью + кросс-мутации) — оба
  «НЕ ПРИНИМАТЬ», десять находок, все перепроверены мной, ключевые на живом
  стенде. Заход исправлений закрыл их; две находки моего собственного разбора
  диффа (`redact` не покрывал `socks5://`, IPv6-хост терял скобки) правил я сам.
  Приёмка: 12 критериев (AC-519 гоняю руками — сторож `accept_run.py` режет
  `rm -rf /tmp/…` по подстроке), 273 теста, гейт 91/91 по коду возврата, семь
  моих собственных мутаций — все убиты на своей строке проверки, живой прогон
  через прокси с обязательной аутентификацией: `user:S3cr3tPw` доехали у обоих
  провайдеров, в JSONL пароля нет.
  **Осталось незамеренным:** эффект прокси на настоящих целях. Машинерия есть,
  замера нет ни одного — нужны креды (строка ниже).

- [x] **07.09.2026 — веха M4 принята и влита.** Обходные входы (`wayback`, `rss`)
  и состояние `not_measured`. Исполнитель Codex, ревью и мутации — Grok.
  **Первый случай в проекте, когда провайдер добавляет цель:** входы взяли обе
  цели за Cloudflare, где `curl` получает 403, тогда как у всех четырёх браузеров
  incremental был ноль. Числа — в `docs/research/03-stand-b-results.md`.
  Живой стенд нашёл дефект, которого не было в тестах: входы ходили с
  `Python-urllib` и получали 403 (правка моя, `34cebee`). Перекрёстный
  мутационный прогон Grok сорвался трижды (таймаут и два обрыва по лимиту ходов);
  четыре выживших мутанта нашёл сам по его наводке, заход исправлений их закрыл.
  Итог: 231 тест, 72 мутанта.

- [x] **07.09.2026 — веха M3 принята и влита.** Детектор челленджа и правило
  эскалации. Исполнитель Grok, ревью и перекрёстные мутации — Codex: два дефекта
  кода (одиночный маркер в прозе давал ложный челлендж; captcha-атрибут решал
  сам) и шесть дыр в тестах, один заход исправлений закрыл всё. Своим чтением
  добавил третий: закомментированный `<title>` побеждал настоящий, потому что
  `_title()` берёт первое совпадение — решающее правило теперь берёт заголовок
  строгой выборкой. Происхождение каждого правила записано машинно
  (`RULE_PROVENANCE`), неизмеренное правило не может решать в одиночку. Итог:
  196 тестов, 55 мутантов, на живой цели `bizprofile.net` 403 + `suspected` +
  семь правил. 🚨 Grok правил `TASKS.md` и `CHANGELOG.md` вопреки «Не трогать» —
  файлы из ветки при вливании выброшены, записи сделаны заново: чужая правка
  журнала выглядит рутинным обновлением документации и молча откатывает
  параллельную работу координатора.

- [x] **06.09.2026 — веха M2 принята и влита.** Исполнитель Codex, спека
  `docs/specs/m2-runner.md`. Ревью и перекрёстные мутации — Grok: ревью без
  находок, мутационный прогон дал шесть выживших (все — дыры в тестах), заход
  исправлений `ad6d038` закрыл их. Итог: 137 тестов, 32 мутанта, гейт 8/8 и 6/6.
  **Живой стенд нашёл то, чего не увидели ни гейт, ни мутации, ни оба ревью:**
  адаптер pydoll не давал chromium флаги песочницы, и все 24 ячейки падали по
  таймауту (`209e3f5`, плюс тест и мутант 32). Первый замер тёплого режима:
  браузер, поднятый заранее, дешевле холодного старта в 40 раз.

- [x] **06.09.2026 — фаза 1 измерена, вердикт написан.**
  `docs/research/04-phase1-verdict.md`. В стеке остаются: обходные входы, HTTP с
  браузерными заголовками, один браузер `patchright`, смена egress, человек.
  Исключены `curl_cffi`/`primp` (incremental 0), ванильный playwright, camoufox
  (в 11 раз тяжелее без единой своей цели), pydoll (таймауты на обеих CF-целях).
  Главный вывод: **браузер окупается рендерингом, а не обходом блокировок**.

- [x] **06.09.2026 — веха M1 принята и влита** (`b732f18`). Исполнитель Grok, два
  независимых прохода Codex (ревью + перекрёстные мутации) нашли 5 дефектов кода и
  6 дыр в тестах, один заход исправлений закрыл всё. Итог: 75 тестов, 17 мутантов,
  все убиты; проверено в чистом контейнере `python:3.14-slim` и на живом стенде.
  Главный дефект — `evaluate()` считал успехом `403` со строкой ожидания в теле.

- [x] **06.09.2026 — разведка и инвентарь кандидатов.** `docs/research/01-candidates.md`:
  25 кандидатов сверены с PyPI/GitHub API. Шесть отсеяны без единого запроса — по
  мёртвости или по лицензии (AGPL/GPL в сетевом сервисе). Затравочный чат владельца
  ошибся в версии или дате у КАЖДОГО проверенного кандидата; ни одно число из него
  в проект не перенесено.
- [x] **06.09.2026 — план замера.** `docs/BENCHMARK_PLAN.md`: два раздельных стенда
  (способности против проходимости), 12 сценариев, признак успеха — sentinel, а не
  HTTP 200, incremental coverage как главный выход.
- [x] **06.09.2026 — список боевых целей.** `bench/targets/targets.toml`, согласован
  владельцем.
