#!/usr/bin/env python3
"""Run the stdio adapter from a checkout, including through a symlink."""
from pathlib import Path
import sys

sys.path.insert(0, str(Path(__file__).resolve().parents[1]))

from gateway.mcp_stdio import main

sys.exit(main())
