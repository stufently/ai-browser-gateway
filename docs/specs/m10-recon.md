# M10 — короткая разведка продуктового входа и API

Исполнитель разведки — явный Grok. M9 принята координатором и опубликована:
BASE_SHA f3c391371b8cf4d4718fad4013bd051b1e5874a2.
Работать только в /home/user/exec-clones/abg-m10-recon-20260914,
read-only, без pull/push/коммитов, продукт/тесты не писать и не менять.
Последний фактический Grok weekly5%, 5h/reset неизвестны. Spark исчерпан до
20.09.2026 15:59; не запускать Spark/auto/Opus или дополнительные модели.

Цель владельца: рабочий шлюз. Определение готовности прочитай в
/home/user/gitlab/9qw/tg-claude-userbot/tmp/abg-coord-task-20260914.md.
Транспорт M9 уже сделан и принят: fetch_page, BenchFetcher, реальное тело,
deadline, env proxy isolation, живой Docker и17независимых мутаций cx.
Не повторять разведку M9, её suite/гейты/ревью/бенчмарки. Исходные факты:
/home/user/.cache/abg-coord-20260914/recon-result.md и
/home/user/.cache/abg-coord-20260914/egress-readiness-result.md.

Дай факты и короткую рекомендацию для следующей спеки:

1. По точным сигнатурам/телам gateway models/plan/engine, fetch_page и probe
   покажи минимальные точки расширения: продуктовый URL без sentinel,
   входы→HTTP→patchright→scrapling→egress→needs_human. Старый benchmark,
   строгий M7/M9 контракт и замороженный tests/probe_m9_transport.py надо
   сохранить. Не предлагай фальшивый sentinel или синтетическую страницу.
   Обоснуй явный новый content-only режим/отдельную границу, если это нужно.
2. Сверь правило эскалации с существующими docs/research/04-phase1-verdict.md
   и05-scrapling.md: какие переходы подкреплены измерениями, где была другая
   точка egress и нельзя объявлять новый результат измеренным. Владелец уже
   требует ступень Scrapling; сравнительный замер двух egress остаётся
   обязательным, но не устраивай новую большую серию замеров в этой разведке.
3. Что безопасно считать успешной доставкой без sentinel: HTTP/challenge/
   непустое реальное содержимое; какие ограничения остаются. Отсутствующие
   RSS entrance/egress profile сейчас дают not_measured, M7 next_step на нём
   падает. Новый продуктовый вход должен безопасно пропускать/отказывать и
   оставлять trace. Валидация типов URL/формата/возраста/бюджета до запуска
   Docker: JSON числа/bool/list вместо URL не должны уходить в транспорт.
4. Контур HTTP API с токеном и общим лимитом параллельных браузеров:
   какой минимальный сервер/формат ответа/ошибок, где считать deadline с
   ожиданием semaphore, как обслужить trace и text/html/markdown. Не писать
   реализацию; перечислить конкретные файлы и необходимые зависимости.
   Новые версии зависимостей не придумывать: если предлагаешь пакет, проверь
   его версию/совместимость по первичному источнику и дай улику. Если без
   новых зависимостей разумно — так и сказать. OpenAI/LLM API здесь не нужен.
5. Read-only прочитай ~/.claude/skills/cf-fetch/fetch.sh: точные режимы
   url [text|html|markdown|links|meta], default text, формат stdout/ошибок.
   Предложи CLI поверх нашего API с совместимым набором режимов. Существующий
   cf-fetch не менять/переключать. MCP опционален; не добавлять без пользы.
6. Для будущего сервиса учти обнаруженный путь Docker mount: probe source
   интерпретирует HOST daemon, а API будет внутри контейнера. В M9 live
   использован одинаковый абсолютный путь дерева снаружи/внутри. Дай
   конкретный вариант доставки версионного probe без монтирования socket
   или всего репозитория в контейнеры провайдеров. Лимит/cleanup должны
   относиться только к своим контейнерам. Сервис/прокси/healthchecks сейчас
   не создавать, никаких production env/кредов.

Предпочтительный следующий результат — продуктовый запрос+API+CLI; затем
отдельно сервис, пулms1-15 и замеры. Оцени, умещается ли первый блок в одну
формализуемую веху и review diff<100000байт, или нужен меньший проверяемый
блок. Это рекомендация по границам, не самостоятельная реализация.

Выход: /home/user/.cache/abg-coord-20260914/m10-recon-result.md.
Факты/неизвестное/минимальная рекомендация, команды/cwd/rc/логи/SHA кода и
артефактов. Не огромный пересказ репозитория. Любые исполняемые проверки
продукта только Docker1002:1002; host Python только оркестрация. Без sudo,
host установок, .env/секретов, чужих рабочих деревьев и сервисов. При блокере
дать конкретную улику; не менять код и не запускать реализации/другие модели.
