# M10 — измерить два egress перед продуктовой лестницей

Исполнитель: явный Grok, без Spark/auto/Opus/других моделей.
Последний факт Grok weekly5%, 5h/reset неизвестны; Spark исчерпан до
20.09.2026 15:59. Не делать quota polling.
Клон /home/user/exec-clones/abg-m10-recon-20260914, read-only,
BASE_SHA f3c391371b8cf4d4718fad4013bd051b1e5874a2.
Не менять продукт, тесты, git-дерево, теги образов, чужие сервисы. Не пушить.
Это ограниченный обязательный сравнительный замер из п.5 задачи владельца,
не новая исследовательская программа. Нужны числа для выбора перехода при403.

## Уже известно

M9 принята, настоящий fetch_page выдаёт содержимое/метрики и строгий sentinel.
M8: docs/research/05-scrapling.md, старый IP198.51.100.30, бюджет120с;
Scrapling добавил bizprofile после patchright403. На текущем IP это неизвестно.
Не повторять полную разведку/тестовые сьюты/ревью M9.
Источники: m10-recon-result.md и egress-readiness-result.md в
/home/user/.cache/abg-coord-20260914/.
Readiness уже проверила direct192.0.2.10 и ms1→198.51.100.21,
ms8→198.51.100.22, ms15→198.51.100.31. Для сравнения взять direct и ms1.

## Замер

1. Зафиксируй SHA дерева, targets/probe и ID трёх текущих образов curl,
   patchright, scrapling. Используй неизменённый принятый fetch_page,
   исходные URL/expect всех valid=true целей targets.toml: 6целей.
   Тот же код, образы, настройки/headers и budget_ms=120000 на обоих egress.
   3провайдера ×6целей ×2egress =36 холодных попыток, по одной, без retry.
   Browser-провайдеры строго последовательно. Между обращениями к тому же
   целевому hostname не менее30с; можно обойти все6целей до следующего круга.
   Не расширять матрицу, не оптимизировать fingerprint, не обходить логин.
2. В начале подтвердить IP обоих маршрутов одной лёгкой echo-попыткой
   (timeout10с), в Docker1002:1002. Если ms1 не работает — зафиксировать
   блокер, не перебирать пул и не объявлять замер двух адресов состоявшимся.
3. Креды взять программным выборочным разбором двух ключей
   PROXY_LOGIN/PROXY_PASSWORD из /home/user/services/3proxy/.env.
   Образец безопасной оркестрации: cache/egress-probe/run_probe.py.
   Не читать глазами, не source/cat, не печатать значения/URLuserinfo/env.
   URL собирать percent-encoding в памяти; передавать только env по имени,
   никакого секрета в argv. Не создавать production proxies.toml.
4. Продукт/benchmark/helper с импортом продукта — ТОЛЬКО Docker UID1002:1002.
   Host Python — только безопасная оркестрация subprocess/разбор источника.
   Docker socket нужен только оркестратору; Docker daemon трактует bind
   host path: дерево доступно по одному абсолютному пути внутри/снаружи.
   Не монтировать socket/всё дерево/креды в провайдеры: только probe, как M9.
   Не host pip/python imports, не sudo. Собственные временные файлы/логи в
   /home/user/.cache/abg-coord-20260914/m10-egress/ mode0700.
5. Записывать фактические строки результата по попыткам: target/provider/
   egress/time/status/error/challenge/sentinel_found/метрики/версию/ID/SHA.
   Не сохранять полные HTML/тексты/куки/заголовки и не печатать секреты.
   Сохранить команды/cwd/rc, helper scripts, логи/манифест SHA256SUMS.
   Успех benchmark определяет принятый строгий контракт, не ручная оценка.
   Показать отдельно случаи sentinel-found с challenge!=none: продуктовый
   URL-only критерий не может приравнивать их к бесспорному успеху.
6. Короткая таблица по двум адресам, coverage/incremental каждого браузера,
   задержки и рекомендация порядка переходов. Разделить измеренные факты,
   эвристику, unknown; один запрос не гарантия стабильности.
   Нужна практическая рекомендация для продукта, включая полезность Scrapling
   после403 и смены egress. Новые серии измерений не предлагать по умолчанию.

Выход: /home/user/.cache/abg-coord-20260914/m10-egress-result.md и артефакты
m10-egress/. Исследовательский текст/данные для docs/research подготовить в
кэше как готовые файлы (без коммита); будущая реализация включит их в пакет.
Никакого report.json реализации или ревью моделей на эту разведку.
При техническом блокере — конкретная улика, не чинить принятый код самому:
сохрани всё и сообщи координатору. После готового пакета остановиться.
