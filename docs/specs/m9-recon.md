# Разведка ABG перед M9 (не реализация)

Репозиторий /home/user/github/ai-browser-gateway; работай только в выделенном клоне /home/user/exec-clones/abg-ready-recon-20260914. Сначала зафиксируй HEAD. Цель владельца полностью в /home/user/gitlab/9qw/tg-claude-userbot/tmp/abg-coord-task-20260914.md — прочитай целиком.

Дай доказательную подготовку коротких вех: M9 реальный fetcher и измеренная лестница, M10 HTTP API, M11 service/CLI и egress/live readiness (разбиение уточним по фактам). Продуктовый код/тесты не писать. Не запускать других моделей. Не менять живой репозиторий, TASKS.md, CHANGELOG.md, чужие деревья; push/merge/deploy/DNS запрещены. Секреты/.env не читать и не печатать. Никаких sudo, host tmux экспериментов или сигналов чужим процессам. Docker запускай deploy, --user 1002:1002 для bind mounts. Системные зависимости не устанавливать на хост.

Проверь и изложи:
1. Точные контракты gateway и bench.runner.execute, доставка html/text/метаданных, timeout/cancellation, proxy, лестница и измеренное правило. Чего не хватает для реального запроса URL без benchmark sentinel? Какие файлы надо изменить M9? Предложи ограниченный наблюдаемый контракт и критерии, опираясь на тела методов, не догадки.
2. Docker baseline suite: команда, rc, длительность, доступные образы. Подготовь окружение M9 если необходимо, только существующими закреплёнными образами. Проверь один живой контрольный fetch существующим раннером; сохраняй первичные логи отдельно, никаких вымышленных измерений.
3. Read-only факты об stand-host (текущий host/есть ли безопасный ssh доступ), существующем cf-fetch fetch.sh (режимы/аргументы), healthchecks правилах владельца (найди документацию без секретов), источниках proxy ms1-15.example.net (пути и имена ключей, НЕ значения). Запрещено выводить конфиги/секреты, читать .env глазами. Без реального доступа явно unknown. Не менять чужие сервисы.
4. API/service: предложи простой поддерживаемый путь, лимитер браузеров, ограничение бюджета, аутентификация, безопасная доступность сервиса; новые версии проверяй в официальных реестрах. Не делай внешнюю публикацию. Для необратимого и доступа без кредов список за владельцем.
5. Предложи достаточные M9 preflight AC команды baseline; независимые probes/мутации будут отдельным противоположным исполнителем, сам их не подменяй.

Выход: /home/user/.cache/abg-coord-20260914/recon-result.md — кратко факты/неизвестное, файлы:строки, команды с cwd, rc, пути логов, SHA дерева и sha256 артефактов. Логи туда же, не в репозиторий. Заверши ответом с путём и блокерами; не ограничивайся планом и не спрашивай о продолжении. При quota error зафиксируй фактические остатки/reset если доступны, без циклов повторов.
