# M16a-fix5 — независимые мутации девяти новых тестов

18.09.2026. Проверочная задача gk, не реализация. Тесты писала cx-панель;
мутации гоняет противоположный исполнитель.

Клон /home/user/exec-clones/abg-m16a-fix5-mutations-20260918, ветка
m16a-fix5-mutations, origin push DISABLED. SOURCE_SHA
`ff648d8c98851880ac9a99df56fa6934bf7964ce` (FINAL M16a-fix5).
Выход: /home/user/.cache/abg-coord-20260918/m16a-fix5-mutations/.

## Чем эта задача отличается от трёх предыдущих

Веха M16a-fix5 — ТОЛЬКО тесты: продуктовый код не менялся ни в
`bench/providers/docker/probe.py`, ни в `gateway/format_html.py`
(`git diff 63e18d7687e907ac8eee0a80939f66cbeee3aaed ff648d8c98851880ac9a99df56fa6934bf7964ce -- bench/ gateway/` пуст). Она закрывает девять мутантов,
выживших в ТВОИХ ЖЕ трёх прогонах:

- шесть по M16a-fix2 (`/home/user/.cache/abg-coord-20260918/m16a-fix2-mutations/result.md`):
  регистр class-токенов; регистр в источнике парсера; регистр значения
  `render`; наличие против значения `data-sitekey`; отсечение фрагмента `#` в
  `src`; имя параметра `render=` целиком;
- два по M16a-fix3 (`…/m16a-fix3-mutations/result.md`): потеря завершающей `;`
  у клипованной ссылки (вход `&#65;6`) и клипование только первого вхождения
  (два гигантских рефа в одном теле);
- один по M16a-fix4 (`…/m16a-fix4-mutations/result.md`): та же ось `;` в
  продуктовом форматтере.

Поэтому проверка «убивают ли новые тесты эти девять осей» уже сделана
воротами и координатором. **Ценность этого прогона в другом: найти ДЕСЯТУЮ
дыру** — ось, которую новые тесты покрывают, но не различают.

## Что мутировать

Продукт мутаций — код, который покрывают девять НОВЫХ тестов:
`bench/providers/docker/probe.py` (разбор атрибутов виджета, разбор `src`
скрипта и его query, сравнение class-токенов, `_clip_oversized_charrefs`) и
`gateway/format_html.py` (тот же помощник). Прочитать целиком
`docs/specs/m16a-fix5-test-holes.md` и
`git diff 63e18d7687e907ac8eee0a80939f66cbeee3aaed ff648d8c98851880ac9a99df56fa6934bf7964ce`.

⚠️ Девять осей из списка выше уже стоят в воротах и заведомо убиваются —
ПОВТОРЯТЬ их не надо (максимум один контрольный, чтобы убедиться, что мутация
вообще ложится). Мутировать СОСЕДНИЕ решения тех же участков, которых в
списке нет. Ориентиры (не исчерпывающий список):

- разбор `src`: отсечение `?` до `#` и наоборот; `split` по `&` против `;`;
  пустое имя параметра; регистр самого имени `render`; `strip()` вокруг
  значения; `urlsplit` против ручного разбора;
- атрибуты виджета: сравнение имени атрибута с учётом регистра; `data-sitekey`
  в теге, отличном от `div`/`script`; несколько атрибутов сразу; пробелы и
  кавычки в значении;
- class-токены: разделение по любому пробельному символу против пробела;
  пустой `class`; токен как подстрока другого токена;
- `_clip_oversized_charrefs` в обоих файлах: `&#` без цифр; `&#;`; ссылка на
  границе строки; `&` без `#`; символ `#` в тексте; ссылка внутри имени тега;
  ссылка, пересекающая границу `feed` (для `html_to_text` — один вызов, для
  `Document` — тоже; проверить, что нет накопления);
- `<template>`: закрытие без открытия; `</template>` внутри атрибута.

Авторский набор — два модуля, гонять ОБА, каждый своей командой:

`docker run --rm --network none --user 1002:1002 -v "$PWD":/work:ro -w /work -e HOME=/tmp -e PYTHONDONTWRITEBYTECODE=1 -e PYTHONHASHSEED=0 sha256:cad9a2c871761c413caa6fdd6441c783451e740a48aaeba60ae62a8b53525ef6 python3 -m unittest -q tests.test_detect`

`docker run --rm --network none --user 1002:1002 -v "$PWD":/work:ro -w /work -e HOME=/tmp -e PYTHONDONTWRITEBYTECODE=1 -e PYTHONHASHSEED=0 sha256:cad9a2c871761c413caa6fdd6441c783451e740a48aaeba60ae62a8b53525ef6 python3 -m unittest -q tests.test_gateway_format`

15–25 смысловых мутантов, каждый в отдельной копии дерева под выходным
каталогом. Для каждого: id, ось, diff, SHA256 исходника/мутанта/восстановления,
доказательство активации изменённой строки авторским набором, команда/cwd/rc,
лог, упавший тест и строка assertion. Syntax/import error — invalid. Нет вызова
— survivor «непокрыто». Equivalent — только с доказательством.

Шестнадцатеричные ссылки не мутировать: `int(s, 16)` лимитом цифр не
ограничен, такой мутант неубиваем по построению.

## Выход и границы

`result.md` в выходном каталоге: сводка (kill / survivor / equivalent /
invalid), таблица по мутантам, отдельный раздел «новые дыры» с точным входом и
вердиктом SOURCE для каждой. Ничего не чинить.

Не трогать: рабочее дерево `/home/user/github/ai-browser-gateway`, другие
клоны, `/home/user/services/**`, `secrets/**`, чужие контейнеры и образы.
`tests/mutation_gate.py` и `tests/mutation_gate_gateway.py` НЕ запускать: они
правят файлы дерева на месте. Сеть не нужна. Push и merge запрещены.
