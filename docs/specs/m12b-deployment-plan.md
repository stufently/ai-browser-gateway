# M12b — план выкладки после приёмки M12a

15.09.2026. Координаторский план, НЕ execution-спека. BASE, команды AC и
точные имена параметров фиксируются после принятого пакета M12a. Запускать
этот файл через launch_executor нельзя. Реализация сейчас одна — M12a.

## Разрешённый результат

Поручение владельца14.09 и обновлённое решение15.09 разрешают scoped deploy
в /home/user/services/ai-browser-gateway, приватную конфигурацию из имеющихся
источников и deployed-прогон. Дополнительного разрешения на эти действия нет
в очереди. Код и исправления пишет исполнитель, координатор принимает.

Опора: принятая разведка m12/recon-result.md в
/home/user/.cache/abg-coord-20260915, SHA256
ceaf2f6efcad8cb8027fef695bd5903de2ad3140e3e3cc21491eb408ac9543e7.
Повторять её целиком не нужно. Перед записью проверить актуальные только
границы назначения: собственный каталог, свободный порт, существующий check,
принятый release SHA и Docker socket gid. Изменившееся состояние не затирать.

## Последовательность

1. Принятый M12a SHA → неизменяемый release штатным abg-release. Файлы644/755,
   manifest вне release, одинаковый абсолютный bind host/API. Образ собрать
   из принятого Dockerfile; зафиксировать точный image ID и исходные pins.
2. Программно создать приватный token и profiles. abg-provision читает только
   PROXY_LOGIN/PROXY_PASSWORD из /home/user/services/3proxy/.env. Не source,
   не отображать значения; существующие секреты не перезаписывать/ротировать.
3. Применить healthchecks-monitoring skill: сначала list по имени
   ai-browser-gateway-health, затем идемпотентное создание/настройка своего
   check. Предложение timeout120/grace60, channels*. Management credentials
   только для управления, в runtime лишь отдельный приватный pingfile.
4. Запустить свой Compose: loopback127.0.0.1:8765, user1002:1002, API socket
   group по stat, RO release/config; sidecar без socket, token и managementkey.
   Проверить health/auth/CLI, metadata mounts/user/restart/healthcheck.
5. Доказать периодическую работу sidecar через историю pings минимум за два
   цикла без ручного ping. Одна запись success и поле last_ping недостаточны.
6. Проверить все15HTTP-профилей на echo, direct-контроль и no-auth407.
   Сохранить имя профиля/status/egressIP/класс отказа, без proxyURL/userinfo.
   Отдельный отрицательный API-контроль доказывает реальный egress и смену
   выбранного профиля; direct success сам по себе wiring пула не доказывает.
7. Все6valid=true из targets.toml через deployed API/CLI: expected_text
   строго target.expect, budget120000, hostname gap>=30s. Полная матрица
   исходов и trace, включая честные needs_human/ошибки, без ослабления policy.
   Замер локального stand не подменяет deployed coverage.
8. Безопасные числа/версии/команды в docs/research и краткое использование,
   остановка/выбор сохранённого release в README. Финальный пакет с SHA,
   кодами возврата и очищенными логами. Секреты не должны попасть в git,
   аргументы, образы, ответы API или публикуемые артефакты.

## Что уточняется перед execution-спекой

- Фактические интерфейсы Compose/release/live и набор новых tests после M12a.
- Какие команды подготовки/проверки уже приняты и могут быть переиспользованы.
- Нужен ли новый runner: если да, реализация и независимые проверки по обычному
  флоу до исполнения production actions; никаких скриптов координатора,
  подменяющих авторский код вехи.
- Перезапускаемые AC должны различать неполученное доказательство и отказ
  самого сервиса. Внешний challenge не объявлять дефектом среды или успехом.
- Для новых tests — независимые мутации, для кода — Codex+Gemini и v2gate.
  После verify новый дефект уходит в новую веху BASE=прежнийFINAL.

## Границы после M12

Переключение cf-fetch/потребителей, DNS, публичный443, WAF и чужие сервисы
остаются за владельцем. Свой CF-стенд — после основного рабочего пути.
MCP оценить по пользе сверх существующего CLI, не добавлять автоматически.
Адаптивные селекторы относятся к проектам-потребителям и вне скоупа шлюза.
